import { Roboto } from 'next/font/google'
import { Varela_Round } from 'next/font/google';

export const roboto = Roboto({
    weight: '400',
    subsets: ['latin'],
});

export const poppins = Varela_Round({
    weight: '400',
    subsets: ['latin'],
});