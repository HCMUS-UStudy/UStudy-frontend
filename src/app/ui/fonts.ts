import { Roboto, Varela_Round, Quicksand, Nunito } from "next/font/google";

export const roboto = Roboto({
  weight: "400",
  subsets: ["latin"],
});

export const varelaRound = Varela_Round({
  weight: "400",
  subsets: ["latin"],
});

export const quicksand = Quicksand({
  weight: "600",
  subsets: ["latin"],
});

export const nunito = Nunito({
  weight: "500",
  subsets: ["latin"],
});
