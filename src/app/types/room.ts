export type RoomItem = {
  id: string;
  name: string;
};

export type RoomData = {
  content: RoomItem[];
  totalElements: number;
  totalPages: number;
};

export type Room = {
  id: string;
  name: string;
};
